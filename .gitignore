# Дампы чипов и всё производное от них (образы, нарезка банков) — содержимое EEPROM в git не кладём
*.bin

# Рабочая папка Ghidra — генерируется run_analysis.sh, содержит копии банков и проект
ghidra/work/
