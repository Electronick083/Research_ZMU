---
name: board-corrosion-failure
description: Отказ вне EEPROM на блоке ветки -101 (D00669) — влага и окисления под процессорами; факт ветки -101, на -1 не переносить без разрешения
metadata:
  type: project
---

**Отказ D00669 (`-101`) вскрыт и устранён 2026-08-13. Причина: попала ВЛАГА, под процессорами образовались ОКИСЛЕНИЯ.** Ремонт: процессоры заменены, плата отчищена. Блок ожил — [[unit-d00669]].

## Почему это важно

⚠️ **Это факт ВЕТКИ `-101`** (D00669 — `285W0027-101`). Формулировки вида «у ZMU бывает такой отказ» не использовать, пока то же не установлено на `-1` — [[hardware-branch-separation]].

Первый на этой ветке достоверно установленный механизм отказа, лежащий полностью вне содержимого памяти. Единственный другой доказанный у нас диагноз — дефект банка A у [[crash-unit-d00109]], и он с ветки `-1` и совсем другого рода (порча данных в чипе). Установлено: блок может быть мёртв при памяти, проходящей все проверки.

**Наш пайплайн такой отказ не видит в принципе** — ни один уровень A/B/C не смотрит на плату: [[firmware-integrity-pipeline]]. Дампы у D00669 были чисты на всех уровнях, и это было ПРАВИЛЬНОЕ показание, а не промах методики. Вывод «в памяти причины нет» надо читать как «смотри плату», а не как тупик.

## Что делать с оставшимися неисправными блоками

**По ветке `-101`:** у [[unit-d00650]] память проходит все проверки — как и у D00669 до ремонта. Осмотр платы на следы влаги и окислений — доступная проверка, которую дампы дать не могут: пайплайн A/B/C на плату не смотрит.

**По ветке `-1` ([[unit-d00092]]) — ТОЛЬКО С РАЗРЕШЕНИЯ ПОЛЬЗОВАТЕЛЯ.** Коррозия установлена на `-101`, и перенос этого на `-1` — межветочный, то есть требует явного разрешения ([[hardware-branch-separation]], правило 2). Я это правило уже нарушил 2026-08-13, записав осмотр плат сразу по обоим блокам; исправлено в тот же день. **Спросить, прежде чем предлагать.**

⛔ **Это предложение ПРОВЕРИТЬ, а не вывод о причине.** Общее у блоков ровно одно — дампы чисты. Отсюда НЕ следует, что механизм отказа тот же: коррозия установлена только на D00669 и только осмотром платы. Никакой «вероятной причины» по D00650 и D00092 у нас нет, и придумывать её нельзя — [[verify-before-claiming]].

Факт из дампов, который коррозией не объясняется: у D00092 **четыре обнулённых носителя** (остаток кода в банке B, копия дескриптора `@0x3F00`, поле `0x1DA0`, журнал NVM стёрт в `FF`).

## Смежное

Процессоров в блоке два: основной **MC68030** и вспомогательный **MC68302** (интерфейс `0x60000000`). Пользователь сказал «процессоры» во множественном числе; какие именно менялись — не уточнял, не додумывать. При замене MC68302 полезно [[cpu68302-package-sourcing]]: корпуса `CFE20C` (TQFP) и `FC20C` (PQFP) взаимозаменяемы, footprint принимает оба.

## Что это НЕ доказывает

Что родная память D00669 была исправна. Одновременно с ремонтом залиты **все 10 чипов** (массив + config + NVM), поэтому блок ни разу не запускался с родным содержимым на исправном железе. Подробности и оговорки — в [[unit-d00669]].
