# Скрипты проекта ZMU — что для чего

Все пути от корня `D:\Claude\ZMU`. Обновлено 2026-07-20.

---

## Диагностика дампов и сборка образа

### `check_dump.py` — проверка одного дампа EEPROM
```bash
python check_dump.py chip1.bin                # анализ одного дампа
python check_dump.py 512-1_a.bin 512-1_b.bin  # + сличение двух чтений
```
Размер и тип чипа определяет сам по длине файла. Ищет обрывы адресной шины
(циклические повторы с периодом 2^i), залипшие биты данных, замыкания линий
(данные — корреляция пар; адрес — инвариантность к обмену бит), аномалии энтропии.

**Первый шаг для любого нового дампа.** Стабильно неверную ячейку не поймает —
для этого `check_rom_crc.py`.

Протокол достоверности: два чтения подряд, не вынимая чип, должны совпасть байт в байт.

⚠️ **Читать не только `=== ИТОГ ===`.** Обрывы адресных линий в вердикт НЕ выносятся
намеренно: они недетерминированы, и статический порог под них подгонять нельзя (иначе
ложные срабатывания на разреженных чипах). Обрыв виден в разделе «Адресные линии» как
аномально низкий процент различий у одной линии на фоне остальных:
```
A 1 (период 2): различий 199/32768 (0.6%)   <-- обрыв A1; у здоровых линий 72-93%
```
Подтверждается только сравнением двух чтений. Проверено на `512_err1.bin`.

Эталонные результаты на тестовых файлах в `285W0027-1\Dump_D00077_OK\` (прогнано 2026-07-20):

| файл | истинный дефект | что говорит скрипт |
|---|---|---|
| `512-1_a` + `512-1_b` | исправен | ИТОГ чисто, «оба чтения идентичны» |
| `512-3_a.bin` | исправен | ИТОГ чисто (ложных нет) |
| `512_err1.bin` | обрыв A1 | ИТОГ чисто, **но** в разделе адресных линий A1 = 0.6% |
| `512_err2.bin` | обрыв данных D6 | `ЗАМЫКАНИЕ/связь линий ДАННЫХ: D5->D6` |
| `512_err3.bin` | замыкание D6–D7 | `ЗАМЫКАНИЕ/связь линий ДАННЫХ: D6=D7` |
| `512_err4.bin` | замыкание A1–A2 | `ЗАМЫКАНИЕ/перепутка АДРЕСНЫХ линий: A1<->A2` |

По одному файлу обрыв линии данных и её замыкание с соседом неразличимы — поэтому у
`err2` природа названа «связь», а не «обрыв». Это ограничение метода, а не ошибка.

---

### `build_image.py` — сборка образа 512 КБ из 8 чипов
```bash
python build_image.py                                        # по умолчанию 285W0027-1/Dump_D00077_OK -> image_BA.bin
python build_image.py 285W0027-1/Dump_D00109_crash image_D00109_crash.bin
```
Аргументы: `[папка_с_дампами] [выходной_файл]`. Читает `512-N_a.bin` или `512-N.bin`.

Порядок лейнов: `byte0=chip1/2`, `byte1=chip3/4`, `byte2=chip7/8`, `byte3=chip5/6`;
банк B в младших адресах. Порядок перепроверен перебором всех 24 перестановок —
он единственный, при котором в образе находятся осмысленные строки.

Проверено 2026-07-20: пересборка всех трёх блоков даёт **побайтово те же образы**
(md5 совпали с `image_BA.bin`, `image_D00109_crash.bin`, `image_D00636.bin`).

---

### `check_rom_crc.py` — сверка контрольных сумм ПЗУ
```bash
python check_rom_crc.py <config.bin> <image.bin>
python check_rom_crc.py 285W0027-1/Dump_D00077_OK/at28c256_1.bin image_BA.bin
```
Повторяет проверку, которую делает сам прибор при старте. Эталоны берёт из config-чипа.
Код возврата 1 при любом FAIL.

Что проверяет:
- **CRC карты памяти** — эталон `@0x1D24`, покрытие `0x1D2A..0x1D9F`
- **Блоки ПЗУ из записи A** — `start=+0x8`, `end=+0xC` (включительно), `storedCRC=+0x14`,
  CRC-32/BZIP2 над `rom[start..end]`
- Блоки копии `@0x3F08` печатает **справочно**, в итог и в код возврата они не идут:
  прибор эту копию не читает, а у исправных блоков она устарела

Эталонные результаты (на них же проверять после любой правки скрипта):

| блок | результат | код возврата |
|---|---|---|
| D00077 | PASS 5, FAIL 0 | 0 |
| D00636 | PASS 5, FAIL 0 | 0 |
| D00109 | PASS 4, **FAIL 1** (банк A) | 1 |
| D00092 | PASS 5, FAIL 0 (пропущено 9) | 0 |

Команды для сверки (все четыре перепрогнаны 2026-07-21, совпали):
```bash
python check_rom_crc.py 285W0027-1/Dump_D00077_OK/at28c256_1.bin image_BA.bin
python check_rom_crc.py 285W0027-1/Dump_D00109_crash/at28c256-1.bin image_D00109_crash.bin
python check_rom_crc.py 285W0027-101/Dump_-101_D00636_OK/at28c256_1_285W0593-5_9805.bin 285W0027-101/Dump_-101_D00636_OK/image_D00636.bin
python check_rom_crc.py 285W0027-1/Dump_D00092_crash/at28c256-1.bin 285W0027-1/Dump_D00092_crash/image_D00092.bin
```
У D00092 «пропущено 9», а не 8: копия `@0x3F08` у него обнулена целиком, поэтому все её записи
уходят в SKIP как нулевые адреса.

⚠️ **Список слотов `REC_A` захардкожен.** Слоты между экземплярами переставляются, и на блоке
с иной раскладкой цепочки этот список промахнётся. Если результат выглядит странно или записи
не находятся — `walk_config.py` (ниже), он ищет записи по структуре.

⚠️ **Покрытие 88.8% (у SWPN `-021`; у `-018` — 92.5%, границы зависят от версии ПО).**
Оставшиеся 11.2% массива не проверяет ничто — ни скрипт, ни прибор. Там лежит код
предыдущей сборки, и он мёртв: доказано пятью методами, см. `ReadMe.txt`, раздел
«ПОЧЕМУ ДЫРЫ ВООБЩЕ ЕСТЬ».
Границы выводов см. в `ReadMe.txt`, раздел «ЧТО МОЖНО И ЧЕГО НЕЛЬЗЯ УТВЕРЖДАТЬ».

---

### `walk_config.py` — ДОПОЛНИТЕЛЬНЫЙ разбор config-чипа (обход по структуре)
```bash
python walk_config.py <config.bin>                 # обход и разбор одного чипа
python walk_config.py <config1.bin> <config2.bin>  # + сличение по полям
```

⚠️ **Это НЕ замена `check_rom_crc.py`.** Основной инструмент уровня C — `check_rom_crc.py`,
и начинать всегда с него. `walk_config.py` нужен, **когда основной не сработал**:

- не нашёл записей или нашёл не все;
- блок неизвестной комплектации (методика гонялась только на `-1` и `-101`);
- результат основного выглядит бессмысленно (адреса вне ПЗУ, `end < start`) — то есть
  похоже, что он разбирает не записи, а то, что лежит на их месте.

**Зачем.** `check_rom_crc.py` ходит по захардкоженному списку слотов `REC_A`. На известных
блоках он верен, но слоты между экземплярами **переставляются**: у D00092 те же записи лежат
в другом порядке, чем у D00077 (2026-07-21). На блоке с иной раскладкой фиксированный список
промахнётся. Здесь записи ищутся **по структуре**: берётся всё, где сходится self-CRC
(BZIP2 над 18 байтами payload с `+6`), позиция значения не имеет.

Та же грабля уже была в SerialPatcher: v1.0 искала записи по «маркеру» и не работала
с `-101`. Ищем по структуре, а не по позиции и не по сигнатуре.

Что печатает: все найденные записи (строковые и блочные дескрипторы с диапазонами и CRC);
серийник с проверкой CRC-32/BZIP2 и байт-контроля; CRC карты памяти; поле `@0x1DA0`.
В режиме сличения — какие записи переставлены, какие уникальны, и **отдельно различия вне
записей** (эти байты не покрыты ничем: ни уровнем C, ни прибором).

Код возврата 1, если провалилась проверка известного поля (серийник, карта памяти).

Эталонные результаты — **ветка `-1`** (прогнано 2026-07-21):

| блок | записей | серийник | карта памяти | поле `@0x1DA0` | провалов |
|---|---|---|---|---|---|
| D00077 | **13** | `D00077` OK | PASS | `0C28D8E1` | 0 |
| D00109 | **13** | `D00109` OK | PASS | `96F8A22E` | 0 |
| D00092 | **8** | `D00092` OK | PASS | **`00000000`** | 0 |

Эталонные результаты — **ветка `-101`** (прогнано 2026-07-21 с разрешения пользователя):

| блок | записей | серийник | карта памяти | поле `@0x1DA0` | провалов |
|---|---|---|---|---|---|
| D00636 | **10** | `D00636` OK | PASS | `289F7C77` | 0 |
| D00650 | **10** | `D00650` OK | PASS | `289F7C77` | 0 |
| D00669 | **10** | `D00669` OK | PASS | `289F7C77` | 0 |

13 = 8 живых записей + 5 в копии `@0x3F08`. У D00092 копия обнулена целиком, отсюда 8.
Числа **13** и **10** независимо воспроизводят длины цепочек из `ReadMe.txt` — обход их
не знал, шёл по структуре. Положительный контроль методики на обеих ветвях.

⚠️ Поле `@0x1DA0` печатается со списком известных значений, но **назначение его неизвестно**
и выдумывать его нельзя. Что про него установлено: не покрыто ни одной КС; гипотеза «CRC
соседнего диапазона» опровергнута перебором; значение **не поэкземплярное, а по паре
«комплектация + версия ПО»** (`-101`/`-021` → `289F7C77` на трёх блоках, `-1`/`-021` →
`0C28D8E1`, `-1`/`-018` → `96F8A22E`). Прежний вывод «поэкземплярное» был сделан по двум
блокам `-1` и опровергнут прогоном на `-101`.

---

## Разбор прошивки — папка `ghidra\`

Ghidra 12.1.2 (`C:\Tools\ghidra_12.1.2_PUBLIC`) + JDK 21. Скрипты на Java, Ghidra
компилирует их на лету — ставить ничего не надо. Подробности в `ghidra\README.md`.

### `ghidra\run_analysis.sh` — раннер
```bash
cd ghidra
./run_analysis.sh <образ.bin> <config.bin> [Скрипт.java ...]
./run_analysis.sh ../image_BA.bin ../285W0027-1/Dump_D00077_OK/at28c256_1.bin ZmuRefs2.java ZmuWalk.java
```
Режет образ на банки, импортирует, строит карту памяти, гоняет автоанализ (~25 с),
затем указанные скрипты. Проект остаётся в `ghidra\work\proj`.

⚠️ **Гонять на КАЖДОЙ прошивке отдельно** — у D00109 софт другой (банк B расходится с
D00077 на 50–95%, а это код). Вывод с одного образа на другой не переносится.

### Скрипты анализа

| скрипт | назначение |
|---|---|
| `ZmuMap.java` | карта памяти + точка входа. Всегда как `-preScript`, сам по себе не запускается |
| `ZmuRefs2.java` | **основной.** Все ссылки из инструкций в окно `0x0000..0x7FFF` + поиск непосредственных констант. Отвечает на вопрос «читает ли прибор такую-то область config-чипа» |
| `ZmuWalk.java` | для каждой ссылки на `0x1D00` печатает 26 инструкций подряд. Им видно ограничение обходчика цепочки и границы проверяемых блоков — так была найдена CRC карты памяти |
| `ZmuDump.java` | дизасм заданных функций + поиск констант косвенной адресации |
| `ZmuCrcCallers.java` | кто вызывает CRC-движок (`0x2000147C`), аддитивный (`0x20001154`) и сверку в config-чипе (`0x16DA`), с контекстом вызова; плюс обращения к слотам цепочки. Им доказано, что прибор проверяет self-CRC записей |
| `ZmuHoles.java` | **проверка дыр CRC-покрытия четырьмя методами сразу**: ссылки извне, функции внутри дыр и их вызывающие, охват дизассемблера, обе таблицы векторов. Им доказано, что код в дырах мёртв. Границы дыр захардкожены под `-021` — при работе с другой версией ПО править массив `HOLES` |
| `ZmuAt.java` | что реально лежит по конкретному адресу: инструкция, данные или середина другой команды; плюс все ссылки на 16 байт вокруг. Им разоблачаются мнимые `bra`, найденные сырым сканом |
| `ZmuRefs.java` | ссылки на конкретные диапазоны config-чипа. Ранняя версия `ZmuRefs2`, оставлена как есть |

Карта памяти: банк B `0x20000000`, банк A `0x30000000`, config-чип `0x00000000`
(код бьёт по нему `abs.w`: `lea (0x1d00).w`), ОЗУ `0x10000000`/`0x40000000`,
HW `0x60000000`, I/O `0xE0000000`/`0xE1000000`. Entry `0x20001040`.

⚠️ Автоанализ покрывает ~четверть массива, поэтому **«Ghidra не нашла ссылок» само по себе
не доказательство** — подкреплять сырым байтовым сканом константы по всему образу.

**Повторный прогон скрипта без переанализа** (проект уже собран, ~10 с вместо ~25):
```bash
"C:/Tools/ghidra_12.1.2_PUBLIC/support/analyzeHeadless.bat" "$(cygpath -w work/proj)" ZMU \
  -process bankB.bin -noanalysis -scriptPath "$(cygpath -w .)" -postScript ZmuAt.java
```

⚠️ **Границы CRC-блоков зависят от ВЕРСИИ ПО.** У `-021` первый блок кончается на `0x2002976F`
(покрытие 88.8%), у `-018` — на `0x2002E08F` (92.5%). Поэтому дыры у разных прошивок разные,
и `ZmuHoles.java` перед прогоном на чужой версии надо перенастроить.

`ghidra\work\` — рабочая папка (банки, config, проект Ghidra). Генерируется, в неё не писать.

---

## Утилита смены серийника — папка `SerialPatcher\`

### `SerialPatcher\ZMU_Serial_Patcher_v1.2.exe` — рабочая версия
GUI. Меняет Serial No во всех identity-записях config-чипа и пересчитывает защиту:
CRC32/BZIP2 `@+0x18` и байт-контроль `sum(ASCII)&0xFF` `@+0x1D`. Длина серийника — 6 символов.
Показывает HWPN/SWPN, ведёт окно-журнал.

Записи ищет **по структуре** (HWPN + сходящаяся self-CRC + валидный Serial No), а не по
«маркеру» `B2 54 5C 51 02 BF 00 02` — тот оказался self-CRC конкретной комплектации и на
`-101` не находил ничего. Поэтому v1.0 с комплектацией `-101` не работала.

Менять серийник **только этой утилитой** — она пересчитывает всё сама.
Смена серийника подтверждена на железе: устройство приняло новый S/N и осталось рабочим.

- `SerialPatcher\serial_patcher_v1.2.py` — исходник актуальной версии
- `SerialPatcher\CHANGELOG.md` — история версий
- `SerialPatcher\Old_vers\` — **архив предыдущих версий. Не трогать.**
  Нужен только при обновлении приложения

---

## Порядок работы с новым блоком

1. Считать все чипы **по два раза** → `check_dump.py` на каждый (физика чтения)
2. `build_image.py <папка> <образ.bin>` (сборка 512 КБ)
3. `check_rom_crc.py <config.bin> <образ.bin>` — **основной**, уровень C: массив против
   эталонов из config, покрытие 88.8%
4. `walk_config.py <config.bin>` — **дополнительный**: разбор config по структуре.
   Гоняется всегда, не только при сбое: два инструмента контролируют друг друга, см. ниже
5. При аномалии — `ghidra\run_analysis.sh` на **эту** прошивку

Ключевые факты по железу и открытые вопросы — в `ReadMe.txt`.

### ⚠️ Зачем гонять оба: захардкоженный список слотов — это РЕПЕР

`check_rom_crc.py` ходит по фиксированному `REC_A`, `walk_config.py` ищет записи по
структуре. Это **намеренная избыточность, и убирать хардкод НЕЛЬЗЯ.**

Причина: при сдвиге байтов в чипе структурный поиск честно найдёт записи на новых местах
и отрапортует «всё сходится» — сдвиг для него невидим, потому что self-CRC переезжает
вместе с записью. Обнаружить сдвиг можно только по расхождению с фиксированным репером.
Скрипт без хардкода теряет эту способность.

Как читать расхождение двух инструментов:

| что видно | вывод |
|---|---|
| записи на тех же смещениях, что в `REC_A` | согласовано, вопросов нет |
| записи те же, но **разложены по другим слотам того же набора** | ШТАТНАЯ перестановка (так у D00092 против D00077) |
| **все** записи смещены на одну и ту же дельту | подозрение на СДВИГ БАЙТОВ — разбираться, это не перестановка |
| `walk_config` нашёл записи, `check_rom_crc` — нет | раскладка чужая: доверять структурному, `REC_A` не годится для этого блока |
| `check_rom_crc` разобрал, `walk_config` не нашёл | на месте слотов лежит НЕ запись (self-CRC не сходится) — основной разбирает мусор |

Различать перестановку и сдвиг по критерию: **перестановка** переставляет записи внутри
того же набора смещений, **сдвиг** двигает все на одинаковую дельту.
